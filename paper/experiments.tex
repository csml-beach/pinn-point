\section{Experiments}

This section should report only curated results that have been promoted from raw run directories under \texttt{outputs/} into the paper-facing \texttt{artifacts/} tree. The current repository already supports multi-method ablations, hyperparameter sweeps, and summary plotting, but the final paper still needs a frozen protocol and a curated metrics bundle.

\paragraph{Planned evaluations.}
\begin{itemize}
    \item Main comparison across adaptive, random, quasi-random, and residual-based resampling methods.
    \item Sensitivity study for RAD hyperparameters and resampling period.
    \item Point-budget or compute-efficiency comparison at matched training conditions.
    \item Seed-aggregated summary plots and final-error tables for the manuscript.
\end{itemize}

\paragraph{Artifact targets.}
\begin{itemize}
    \item Figures in \texttt{artifacts/figures/}
    \item Tables and CSV summaries in \texttt{artifacts/metrics/}
    \item Supplementary animations in \texttt{artifacts/animations/}
\end{itemize}
